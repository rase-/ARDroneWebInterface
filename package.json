{
  "name": "ardrone_service",
  "description": "A nice HTTP interface for sending commands to the AR drone",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "3.x",
    "redis": "0.8.x"
  }
}